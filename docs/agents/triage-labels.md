# Triage labels

Use these labels in Linear issue labels:

- `needs-triage`
- `needs-info`
- `ready-for-agent`
- `ready-for-human`
- `wontfix`

The Second Brain team currently has `ready-for-agent`; create the remaining labels when a workflow needs them.
